/* eslint-disable @typescript-eslint/no-explicit-any */
const initialState = {
  list: []
};

export default (state = initialState, action: any) => {
  switch (action.type) {
    default:
      return state;
  }
};
